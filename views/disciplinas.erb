<title>Hero Sheet: <%= @char.gsub('beastlord','señor de las bestias').capitalize %></title>
<%= erb :menu %><br>

<h1>Habilidades del <%= @char.gsub('beastlord','señor de las bestias').capitalize %></h1>
<table id='table-2'>
	<thead>
		<tr><% disciplinas.each do |d| %>
				<th><%= d.capitalize %></th>
			<% end %>
		</tr>
	</thead>
	<tbody>
		<% send(@char+"s").each do |c| %>
			<tr><td><%= c[:id] %></td>
				<td><% img_path = "'../images/skills/#{@char}/#{c[:name]}.png'" %>
					<img src=<%=img_path%> alt=<%= c[:name] %>, id='icon' />
				</td>
				<td><%= c[:type].capitalize %></td>
				<td><%= c[:name] %></td>
				<td><i><%= c[:description] %></i></td>
				<td><%= "In progress" %></td>
				<td><%= "In progress" %></td>
			</tr>
		<% end %>
	</tbody>
</table>
<br>